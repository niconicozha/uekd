torch>=2.0
numpy>=1.24
scikit-learn>=1.3
tqdm>=4.65

# Optional: only needed for feature extraction from raw text/images
transformers>=4.40
pillow>=10.0

# Optional: only needed for GossipCop domain clustering with Sentence-BERT
sentence-transformers>=2.7
